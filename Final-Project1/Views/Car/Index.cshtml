@model CarVM
<style>
    .tm-img-1-container {
       
        height: 160px;
        overflow: hidden;
        padding:5px;
        display: inline-block;
        vertical-align: top;
    }

        .tm-img-1-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
        }
</style>

<section class="container margin-bottom-50">
    <div>
        <h2 class="col-lg-12 text-center text-uppercase margin-bottom-30">Hayalinizdeki Araca Bir Adım Daha Yaklaşın</h2>
        <p class="col-lg-12 text-center margin-bottom-30">En güvenilir markalar, en uygun fiyatlar ve satış sonrası destekle araç sahibi olmak artık çok kolay. Size en uygun seçeneği birlikte bulalım.</p>
       <div>
            <div class="tm-img-1-container col-md-3">
            <img src="/img/Honda-Civic-Sedan-LPG-1.jpg" alt="Image description">
            <p class="tm-img-1-description">Honda</p>
        </div>
        <div class="tm-img-1-container col-md-3">
            <img src="/img/Fiat_Egea_Cross.jpg" alt="Image description">
            <p class="tm-img-1-description">Fiat</p>
        </div>
        <div class="tm-img-1-container col-md-3">
            <img src="/img/kia-rio-facelift.jpg" alt="Image description">
            <p class="tm-img-1-description">Kia</p>
        </div>
        <div class="tm-img-1-container col-md-3">
            <img src="/img/mercedes-3417100_640.jpg" alt="Image description">
            <p class="tm-img-1-description">Mercedes</p>
        </div>
        </div>
    </div>
</section>


<section class="container margin-bottom-50">
    <div class="tm-overflow-hidden row">
        <div class="tm-gallery col-lg-9 col-md-9 col-sm-8 col-xs-12">

            @if (Model.Brands != null && Model.Brands.Any())
            {
                foreach (var car in Model.ChildBrands)
                {
                    @*<div class="tm-item-container col-md-4">
                    <div class="tm-item">
                        <img src="@car.PictureUrl" alt="@car.Name" />
                        <div class="tm-item-price-container">
                            <a asp-controller="Car" asp-action="Index" asp-route-id="@car.Id">@car.Name</a>
                            <span class="tm-item-action">Araç Detayları</span>
                        </div>
                    </div>

                </div>*@
                    <div >
                        <div class="col-sm-6 col-md-4">
                            <div class="thumbnail">
                                <img src="@car.PictureUrl" alt="@car.Name">
                                <div class="caption">
                                    <h3>@car.Name</h3>
                              
                                    <p><a asp-controller="Car" asp-action="Detail" asp-route-id="@car.Id" class="btn btn-primary" role="button"> Araç Detayları</a></p>
                                </div>
                            </div>
                        </div>
                    </div>
                }
                //Deneme 
            }
            else
            {
                <p>Araç bulunamadı.</p>
            }

        </div>


                <aside class="tm-gallery-aside col-lg-3 col-md-3 col-sm-4 col-xs-12">
                    <nav class="tm-gallery-nav">
                        <h2 class="tm-gallery-nav-title">Markalarımız <i class="fa fa-caret-up"></i></h2>
                        <ul>
                            @if (Model.Brands != null && Model.Brands.Any(b => b.ParentId == null))
                            {
                                var firstBrand = Model.Brands.FirstOrDefault(b => b.ParentId == null);
                                foreach (var brand in Model.Brands.Where(b => b.ParentId == null))
                                {
                                    <li class="@(firstBrand == brand ? "active" : "")">
                                        <a asp-controller="Car" asp-action="Index" asp-route-id="@brand.Id">@brand.Name</a>
                                    </li>
                                }
                            }
                            else
                            {
                                <li>Araç Markası Bulunamadı.</li>
                            }
                        </ul>
                    </nav>
                </aside>

                @*Galeri Ksımı*@
            </div>

            <!--banner-->
            <div class="tm-banner">
                <h2 class="tm-banner-title">Maecenas</h2>
                <p class="tm-banner-description">Donec pede justo, fringilla vel, aliquet nec, vulputate eget, arcu. In enim justo, rhoncus ut, imperdiet a, venenatis vitae, justo. Nullam dictum felis eu pede mollis pretium</p>
                <a href="#" class="tm-banner-link"><i class="fa fa-play"></i></a>
            </div>
</section> 